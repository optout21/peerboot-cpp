v0.1:
- PeerBoot net peers connections
- configured PB peers
- NO PB peer propagation, no fixed well-known peers
- connection from client
- storage of peer info
- propagation of peer info (send and receive)
- NO Query
- NO memory or bandwith caps
- TCP connections
- NO UDP

- Serialization/deserialization of messages
- visitor for message types
- Listen TCP, handle net messages
- MsgStore, forward only once, handle only once
- replacable time source, for testing
- Forward query msg -- currently there is no limit on it, so it would go endless, PeboNet::queryFromPeboPeer